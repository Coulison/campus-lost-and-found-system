# Generated by scripts/install.sh — run it after cloning to recreate these.
# They are absolute symlinks into this working tree, so they are not portable.
/.opencode/
/.claude/
/.cursor/
/AGENTS.md
/CLAUDE.md
/opencode.json

# Runtime output (each run creates output/<slug>/)
/output/*
!/output/.gitkeep

# Environment variables & secrets
.env
*.pfx

# OS / editor cruft
.DS_Store
.qodo/
