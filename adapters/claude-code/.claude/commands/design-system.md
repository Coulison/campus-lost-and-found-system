---
description: Generate a design system from the most recent PRD + ADR.
---
Read the latest `output/<slug>/prd.md` and `adr.md`. Use the designer subagent (via the Task tool) to write `design-system.md` beside them following the `design-system-authoring` skill, then validate once with the validator subagent. Report the design-system.md path.
